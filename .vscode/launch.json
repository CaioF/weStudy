{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Server",
            "program": "${workspaceRoot}/server.js",
            "cwd": "${workspaceRoot}",
            "console": "integratedTerminal"
        }        
    ],
    "compounds": [
        {
            "name": "Server/Client",
            "configurations": ["Server"]
        }
    ]
}